{
    "name": "recipe-processor",
    "version": "1.0.0",
    "description": "Lambda function for processing automation recipes",
    "main": "index.js",
    "dependencies": {
      "aws-sdk": "^2.1001.0",
      "adm-zip": "^0.5.10"
    },
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "zip": "zip -r ../lambda_function.zip ."
    }
  }